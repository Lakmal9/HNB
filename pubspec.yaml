name: interface_app
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  animated_size_and_fade: ^1.0.3
  flutter_spinkit: ^3.1.0
  flutter_custom_clippers: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - Images/

  fonts:
    - family: Roboto
      fonts:
        - asset: Fonts/Roboto-Regular.ttf
